# Healance AI — Symptom Disease ML service
#
# Use minimum-version ranges (>=) rather than strict pins so the team can
# reinstall cleanly on newer Python versions (tested on Python 3.14.2).
# Versions below are the floor we target; newer compatible releases are fine.

pandas>=2.2
numpy>=2.2
scipy>=1.16
scikit-learn>=1.6
joblib>=1.4
xgboost>=3.0
